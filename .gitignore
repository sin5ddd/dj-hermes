# Rust
/target/
**/*.rs.bk
*.pdb

# Cargo (binary プロジェクトのため Cargo.lock は追跡する)
# Cargo.lock

# OS
.DS_Store
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# Editors / IDEs
.idea/
.vscode/
*.swp
*.swo
*~
.vim/

# Agent / local tooling
.sqlew/
.claude/settings.local.json
.junie/
.mcp.json
mcps/
terminals/
agent-tools/

# Jujutsu leftover (do not colocate; Git LFS). Local only if present.
.jj/

# Env / secrets
.env
.env.*
!.env.example

# Logs / temp
*.log
*.tmp
tmp/
temp/
__pycache__/
*.pyc

# Audio debug dumps / scratch bounce (not sample kits)
*.wav.raw
/out/
/recordings/

# LUFS factory runs (WAV + generated reports)
eval/lufs/runs/

# Sample kits (bundled + synthesist drops) are tracked via Git LFS — see .gitattributes.
# Do not ignore samples/**/*.wav.
