# Rust
/target/
**/*.rs.bk
*.pdb

# Cargo (binary プロジェクトのため Cargo.lock は追跡する)
# Cargo.lock

# OS
.DS_Store
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# Editors / IDEs
.idea/
.vscode/
*.swp
*.swo
*~
.vim/

# Agent / local tooling
.sqlew/
.claude/settings.local.json
.junie/
.mcp.json
mcps/
terminals/
agent-tools/

# Jujutsu (colocated with .git; local only — do not commit)
.jj/

# Env / secrets
.env
.env.*
!.env.example

# Logs / temp
*.log
*.tmp
tmp/
temp/

# Audio debug dumps (if any)
*.wav.raw
/out/
/recordings/

# User sample kits at samples/ root (bundled kits live in samples/<name>/*.wav folders)
/samples/*.wav
