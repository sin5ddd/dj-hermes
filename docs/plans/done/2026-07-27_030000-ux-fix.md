## file loader

- [x] 指定がなければワーキングディレクトリの `songs/*.strudel` を参照する
- [x] 拡張子 `.strudel` または `.txt` は省略OK
    - [x] 重複している場合は `.strudel` を優先

### 実装（2026-07-27）

- `song::resolve_song_path` / `song_path_candidates`（`src/song.rs`）
- 利用箇所: CLI `play` / `dj`、REPL `a load`、HTTP `POST /song/load`（MCP 経由含む）
- 裸名 `smoke` → `songs/smoke.strudel` → `songs/smoke.txt` → cwd 同名
- `..` は拒否（sanitize と共通）

## Source Code on TUI

- [ ] JavaScriptとしてシンタックスハイライト（見送り: 表示崩れのため Task 24 時点の描画に戻した）
- [ ] 行を折り返しして表示（同上）

ライブ再生中 atom の反転強調（既存）はそのまま。
